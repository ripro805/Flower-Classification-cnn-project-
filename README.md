# Flower-Classification-cnn-project-
Deep Convolutional Neural Network (CNN) for multi-class flower classification using PyTorch. The model includes 4 convolutional blocks with batch normalization, ReLU activation, and max pooling, followed by fully connected layers with dropout for regularization. Trained on Kaggle flower dataset for accurate image classification.
